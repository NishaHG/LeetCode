class Solution:
    def reverseVowels(self, s: str) -> str:
        left,right=0,len(s)-1
        vowels=set('aeiouAEIOU')
        list_s=list(s)

        while left<right:
            while left<right and list_s[left] not in vowels:        
                left+=1

So if the current s_list[left] is not a vowel, it will not even go to check the right side yet.
            while left<right and  list_s[right] not in vowels:
                right-=1

            list_s[left],list_s[right]=list_s[right],list_s[left]
            left+=1
            right-=1

        return "".join(list_s)
       


        
