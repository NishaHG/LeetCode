class Solution:
    def findErrorNums(self, nums: List[int]) -> List[int]:
        n=len(nums)
        num_set=set()
        dup=-1

        for num in nums:
            if num in num_set:
                dup=num
            num_set.add(num)


        total=(n*(n+1)) // 2
        # sum(nums)=total+dup-miss
        miss=total+dup-sum(nums)

        return [dup,miss]
        

        
