class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        xoar=0
        n=len(nums)
        for i in range(n+1):
            xoar^=i
        for num in nums:
            xoar^=num
        return xoar
        
